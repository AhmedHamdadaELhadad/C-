class Rectangle {
	private:
		float length, width;
	Rectangle();
	Rectangle(float l,float w );
		float area();
			float perimate();
			float get length();
			float set length();
			float get width;  
	float set_width();
};
