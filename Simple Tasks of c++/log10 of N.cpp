#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int n, first, last, sum, digits, nn, a, b;
cout<< "ENTER THE NUMBER \n ";
cin>>n;
last=n%10;
cout<<last<<"\n";
  digits = (int)log10(n);
  cout<<digits;
}
